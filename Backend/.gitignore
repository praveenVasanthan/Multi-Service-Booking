# Dependencies
node_modules/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files
.env
.env.local
.env.*.local
*.local

# Build and distribution folders
dist/
build/
coverage/

# Yarn Plug'n'Play files
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions

# Optional Yarn cache & state
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# TypeScript build output
*.tsbuildinfo

# Editor & IDE settings
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS / Linux / Windows junk
.DS_Store
Thumbs.db

# Lock files (optional — ignore if using only Yarn PnP)
yarn.lock
package-lock.json
pnpm-lock.yaml
